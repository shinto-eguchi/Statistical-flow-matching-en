## English R_SETUP.md :contentReference[oaicite:3]{index=3}

```markdown
# R Setup (for calling R from Python)

`Cox_FM_PH_model1.ipynb` calls R from Python, so you need both **R** and **rpy2**.

## 1) Install R

- Windows/macOS/Linux: Install R from CRAN.
- After installation, verify that the following works in your terminal (or R console):

```bash
R --version